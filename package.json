{
  "name": "ia-aprendizagem",
  "version": "1.0.0",
  "description": "Sistema de IA para auxiliar no aprendizado com chatbot frontend e API backend",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:frontend\"",
    "dev:api": "cd . && source .venv/bin/activate && uvicorn api.main:app --reload --port 49152",
    "dev:frontend": "cd frontend && npm run dev",
    "install:backend": "python -m venv .venv && source .venv/bin/activate && pip install uv && uv pip install -r pyproject.toml",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "",
  "license": "ISC"
}